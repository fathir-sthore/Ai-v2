{
  "name": "fadax-drak-ai",
  "version": "2.1.0",
  "description": "FADAX DRAK AI — Chat Assistant with OpenAI & MongoDB Atlas",
  "main": "server.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed'",
    "gen-secret": "node -e \"console.log('JWT_SECRET=' + require('crypto').randomBytes(64).toString('hex'))\""
  },
  "author": "Selvi Time Daxyinz",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.4.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
